version: "3.8"

services:
  frps:
    build: .
    container_name: frps
    restart: unless-stopped
    ports:
      - "7000:7000"   # FRP control port
      - "8080:8080"   # example exposed service port
      - "7500:7500"   # dashboard (optional)
    volumes:
      - ./frps.ini:/app/frps.ini